<UserControl x:Class="MediCoreHospital.UI.Views.Login.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="RightToLeft"
             Background="#F7F9FC">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.1*" />
            <ColumnDefinition Width="0.9*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#0D1B3A" Padding="48">
            <StackPanel VerticalAlignment="Center">
                <Border Width="110" Height="110" CornerRadius="55" Background="#1C8C8C" Margin="0,0,0,20">
                    <TextBlock Text="MC" FontSize="42" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>

                <TextBlock Text="MediCore" Foreground="White" FontSize="42" FontWeight="Bold" />
                <TextBlock Text="نظام إدارة المستشفى" Foreground="#CFE5FF" FontSize="24" Margin="0,10,0,0" />
                <TextBlock Text="إدارة المرضى، المواعيد، الفواتير، الطوارئ، الصيدلة، المختبر، الأشعة، والتقارير." Foreground="#D7E7FF" FontSize="16" TextWrapping="Wrap" Margin="0,20,0,0" />
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Background="White" Padding="40">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="تسجيل الدخول" FontSize="34" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,20" />
                <TextBlock Text="مرحباً بعودتك إلى منصة MediCore" Foreground="#475569" FontSize="15" Margin="0,0,0,30" />

                <TextBlock Text="اسم المستخدم" Foreground="#334155" FontSize="14" Margin="0,0,0,8" />
                <TextBox x:Name="UsernameTextBox" Height="46" FontSize="16" Padding="12,0" BorderBrush="#CBD5E1" BorderThickness="1" Background="#F8FAFC" />

                <TextBlock Text="كلمة المرور" Foreground="#334155" FontSize="14" Margin="18,18,0,8" />
                <PasswordBox x:Name="PasswordBox" Height="46" FontSize="16" Padding="12,0" BorderBrush="#CBD5E1" BorderThickness="1" Background="#F8FAFC" />

                <CheckBox Content="تذكرني" Foreground="#334155" Margin="0,20,0,0" />

                <Button x:Name="LoginButton"
                        Content="دخول النظام"
                        Height="52"
                        Margin="0,25,0,0"
                        Foreground="White"
                        FontWeight="Bold"
                        FontSize="18"
                        Background="#0D6EFD"
                        BorderThickness="0"
                        Click="LoginButton_Click" />

                <TextBlock x:Name="StatusText"
                           Margin="0,18,0,0"
                           Foreground="#DC2626"
                           FontSize="13"
                           Text="" />

                <TextBlock Text="الإصدار: 1.0.0" Foreground="#64748B" Margin="0,20,0,0" HorizontalAlignment="Center" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
