<UserControl x:Class="MediCoreHospital.UI.Views.Appointments.AppointmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="RightToLeft" Background="#F7F9FC">
    <Grid Margin="24">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="0,0,0,18"><TextBlock Text="المواعيد" FontSize="30" FontWeight="Bold" Foreground="#0F172A"/><TextBlock Text="إدارة مواعيد المرضى والعيادات" Foreground="#64748B" Margin="0,6,0,0"/></StackPanel>
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,18"><StackPanel Orientation="Horizontal"><TextBlock Text="تاريخ المواعيد" VerticalAlignment="Center" Margin="0,0,10,0"/><DatePicker SelectedDate="{Binding SelectedDate}" Height="38" Width="160"/><Button Content="تحديث" Width="100" Height="38" Margin="12,0,0,0" Background="#0F4C81" Foreground="White" BorderThickness="0" Click="Refresh_Click"/><ProgressBar IsIndeterminate="True" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}} Width="90" Height="4" Margin="16,0,0,0"/></StackPanel></Border>
        <Border Grid.Row="2" Style="{StaticResource CardStyle}"><DataGrid ItemsSource="{Binding Appointments}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" BorderThickness="0" RowHeaderWidth="0" AlternatingRowBackground="#F8FAFC"><DataGrid.Columns><DataGridTextColumn Header="المريض" Binding="{Binding PatientName}" Width="*"/><DataGridTextColumn Header="الطبيب" Binding="{Binding DoctorName}" Width="*"/><DataGridTextColumn Header="القسم" Binding="{Binding DepartmentName}" Width="*"/><DataGridTextColumn Header="الوقت" Binding="{Binding StartTime, StringFormat=hh\:mm}" Width="100"/><DataGridTextColumn Header="السبب" Binding="{Binding Reason}" Width="*"/><DataGridTextColumn Header="الحالة" Binding="{Binding Status}" Width="120"/></DataGrid.Columns></DataGrid></Border>
    </Grid>
</UserControl>
